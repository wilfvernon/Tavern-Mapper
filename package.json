{
  "name": "tavern-mapper",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight, single-file tabletop map controller",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && npm run test:unit && node test-suite.js",
    "migrate:legacy": "node scripts/extract-legacy-source.mjs",
    "test": "node test-suite.js",
    "test:unit": "node --test tests/*.test.mjs",
    "pretest": "npm run build && playwright install chromium"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.48.0",
    "pngjs": "^7.0.0"
  }
}
